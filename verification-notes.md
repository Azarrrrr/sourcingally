# Verification Notes

The production journal URL, `https://sourcingally.com/en/blog/`, loaded after commit `4b98c07`. The rendered page includes the updated **Sanity-managed** editorial message and retains the existing local Markdown journal as a fallback. The browser CORS check then returned **HTTP 200** with `access-control-allow-origin: https://sourcingally.com` for the public `sourcing-ally` English article query. A genuine English guide, **How to compare supplier quotes before placing an order**, was published with the stable ID `article--sourcing-ally--en--compare-supplier-quotes-before-ordering`. Headless production checks confirmed the article appears in the live journal list and in the actual Astro detail-route proxy at `https://sourcingally.com/en/blog/sanity-article/?article=compare-supplier-quotes-before-ordering`. The proxy’s `live-sanity-detail` mount is active after rendering, while the existing Markdown collection remains the fallback if a matching public Sanity record is unavailable.
